server {
    listen 80;

    location /static/ {
        root /var/www/;
    }

    location /media/ {
        root /var/www/;
    }

    location / {
        proxy_pass       http://localhost:8000;
        proxy_set_header Host      $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

}